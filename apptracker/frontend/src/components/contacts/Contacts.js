import React, { Component } from "react";

export class Contacts extends Component {
  render() {
    return (
      <div>
        <h1>Contacts List</h1>
      </div>
    );
  }
}

export default Contacts;
